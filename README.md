## Продуктивність: генератор vs "жадібна" версія

Корпус: 20 книг з Project Gutenberg, ~15 МБ сирого тексту, 1 374 875 токенів.

| Підхід                          | Час      | Пікова пам'ять |
|----------------------------------|----------|----------------|
| Генератори (`iter_documents` → `tokenize`) | 8.03 с   | 18.98 МБ       |
| Списки (`list(...)` на кожному кроці)      | 7.14 с   | 86.26 МБ       |

Різниця в пам'яті: **~4.5×**. Час виконання практично однаковий — обидва варіанти
виконують ту саму кількість операцій токенізації; різниця саме в тому, скільки
даних тримається в пам'яті ОДНОЧАСНО.
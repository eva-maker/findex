## Lab 1 — Iterators & the corpus

Корпус: 20 книг з Project Gutenberg (англійська + одна французька — Renan,
"Vie de Jésus", про це нижче). Разом ~15 МБ тексту.

`iter_documents()` читає файли по одному через генератор, не тримає весь
корпус у пам'яті. Спершу різала службовий текст ліцензії Gutenberg по
маркерах START/END, але коли прогнала статистику — в топ-50 нічого дивного
не було, зате коли подивилась на перші 80 символів кожного документа окремо,
у кількох файлах замість тексту книги стояло "Produced by..." або
"Transcriber's Notes" — це окремий шар "сміття" поверх ліцензії, який
маркер START/END не чіпає. Додала ще один фільтр, що відрізає перші абзаци,
якщо вони містять такі службові слова.

`tokenize()` — теж генератор. Апострофи (`don't`) і дефіси
(`state-of-the-art`) тримаю всередині токена, підкреслення (Gutenberg
позначає ним курсив, типу `_Ask_`) прибираю як розмітку. Unicode
нормалізую через NFKC, щоб криві апострофи (’) і прямі (') давали
однаковий токен. 9 тестів на ці правила — `tests/test_tokens.py`.

### Генератор vs списки

| Підхід     | Час    | Пікова пам'ять |
|------------|--------|----------------|
| Генератори | 8.03 с | 18.98 МБ       |
| Списки     | 7.14 с | 86.26 МБ       |

Різниця в пам'яті ~4.5×, а час майже однаковий — логічно, бо кількість
операцій токенізації та сама, різниця тільки в тому, скільки всього
одночасно висить у пам'яті. У жадібній версії одночасно тримаються і весь
список документів, і список з 1 374 875 окремих рядків-токенів — а кожен
рядок в Python це не просто байти слова, а ще окремий об'єкт з накладними
витратами.

Побічний ефект мультимовного корпусу: у топ-50 термів затесались
французькі стоп-слова (de, et, la) — з тієї самої книги Ренана. Не баг,
просто корпус такий.